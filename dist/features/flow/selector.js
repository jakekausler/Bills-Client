export const selectFlow = (state) => state.flow.flow;
export const selectFlowLoaded = (state) => state.flow.flowLoaded;
export const selectFlowError = (state) => state.flow.flowError;
export const selectSelectedAccounts = (state) => state.flow.selectedAccounts;
export const selectFlowStartDate = (state) => state.flow.startDate;
export const selectFlowEndDate = (state) => state.flow.endDate;
